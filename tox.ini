# tox (https://tox.readthedocs.io/) is a tool for running tests
# in multiple virtualenvs. This configuration file will run the
# test suite on all supported python versions. To use it, "pip install tox"
# and then run "tox" from this directory.

# Configuration file for quick / short tests.

[tox]
envlist = lint, py{310,311,312,313}-{base,slow}

[testenv]
# make sure those variables are passed down; you should define 
# either explicitly the RDIFF_TEST_* variables or rely on the current
# user being correctly identified (which might not happen in a container)
passenv = RDIFF_TEST_*, RDIFF_BACKUP_*
setenv =
# paths for coverage must be absolute so that sub-processes find them
# even if they're started from another location.
	COVERAGE_FILE = {envtmpdir}/coverage.sqlite
	COVERAGE_PROCESS_START = {toxinidir}/tox.ini
deps = -r{toxinidir}/requs/base.txt
       -r{toxinidir}/requs/optional.txt
       -r{toxinidir}/requs/test.txt
allowlist_externals = sh
commands_pre =
	rdiff-backup info
# must be the first command to setup the test environment
	python testing/commontest.py
	coverage erase
# write the hook file which will make sure that coverage is loaded
# also for sub-processes, like for "client/server" rdiff-backup
	python testing/coverage_pth.py
commands =
	base: coverage run testing/action_backuprestore_test.py --verbose
	base: coverage run testing/action_calculate_test.py --verbose
	base: coverage run testing/action_compare_test.py --verbose
	base: coverage run testing/action_complete_test.py --verbose
	base: coverage run testing/action_list_test.py --verbose
	base: coverage run testing/action_regress_test.py --verbose
	base: coverage run testing/action_remove_test.py --verbose
	base: coverage run testing/action_test_test.py --verbose
	base: coverage run testing/action_verify_test.py --verbose
	base: sh ./testing/verbosity_actions_test.sh 9 --verbose
	base: coverage run testing/api_test.py --verbose
	base: coverage run testing/arguments_test.py --verbose
	base: coverage run testing/c_test.py --verbose
	base: coverage run testing/connection_test.py --verbose
	base: coverage run testing/eas_acls_test.py --verbose
	base: coverage run testing/errorsrecover_test.py --verbose
	base: coverage run testing/filename_mapping_test.py --verbose
	base: coverage run testing/fs_abilities_test.py --verbose
	base: coverage run testing/hash_test.py --verbose
	base: coverage run testing/increment_test.py --verbose
	base: coverage run testing/iterfile_test.py --verbose
	base: coverage run testing/kill_test.py --verbose
	base: coverage run testing/librsync_test.py --verbose
	base: coverage run testing/location_lock_test.py --verbose
	base: coverage run testing/location_map_filenames_test.py --verbose
	base: coverage run testing/location_map_hardlinks_test.py --verbose
	base: coverage run testing/longname_test.py --verbose
	base: coverage run testing/metadata_test.py --verbose
	base: coverage run testing/rdiff_test.py --verbose
	base: coverage run testing/readonly_actions_test.py --verbose
	base: coverage run testing/robust_test.py --verbose
	base: coverage run testing/rorpiter_test.py --verbose
	base: coverage run testing/rpath_test.py --verbose
	base: coverage run testing/security_test.py --verbose
	base: coverage run testing/selection_test.py --verbose
	base: coverage run testing/setconnections_test.py --verbose
	base: coverage run testing/singletons_log_test.py --verbose
	base: coverage run testing/singletons_stats_test.py --verbose
	base: coverage run testing/time_test.py --verbose
	base: coverage run testing/user_group_test.py --verbose
	base: coverage run testing/utils_buffer_test.py --verbose
	base: coverage run testing/utils_convert_test.py --verbose
	base: coverage run testing/utils_simpleps_test.py --verbose
	# ---- slow suite: benchmarks ----
	slow: coverage run testing/benchmark.py many
    slow: coverage run testing/benchmark.py nested
# can only work on OS/X TODO later
#	coverage run testing/resourcefork_macostest.py

# combine all coverage results and show the summary
	coverage combine
	coverage report

[testenv:lint]
deps = -r{toxinidir}/requs/test.txt
commands_pre=
commands =
	flake8 setup.py src testing tools
	black --check --diff setup.py src testing tools
	bandit -r -ll -c pyproject.toml ./setup.py src testing tools
	mypy --non-interactive --install-types

[testenv:format]
deps = -r{toxinidir}/requs/test.txt
commands_pre=
commands =
	black setup.py src testing tools

[flake8]
ignore =
	# line too long (86 > 79 characters)
	E501
	# line break before binary operator
	W503
	# whitespace before ':' (due to black)
	E203
filename =
	*.py,
	src/rdiff-backup*
exclude =
    .git
    .tox
    .tox.root
    __pycache__
    build
max-complexity = 20

[coverage:run]
parallel = True
source =
	rdiff_backup
	rdiffbackup

[coverage:report]
skip_empty = True
fail_under = 85
sort = Cover
